# Sensor choice & ADC reference — измерения

## 12.09.26 — Stage A sanity check (MPX5700AP, экземпляр из коробки старых деталей)

**Сборка:** MPX5700AP на макетной плате, Vout → AtomS3R G5, GND и Vcc — через
рейки макетки. Без делителя/ОУ/фильтра между датчиком и АЦП (README часть 2).

**Найденные и исправленные проблемы по пути:**
- Нестабильный контакт на рейке GND (прозвонка показывала то OL, то плавающие
  значения) — почини­ли переносом на другую пару отверстий/проверкой рейки,
  довели до стабильных ~0.2–0.3 Ом.
- Датчик стоял **не той стороной** (ножки перепутаны местами) — из-за этого
  Vout был заниженным (~0.47В) и не реагировал на давление вообще. После
  переворота — датчик сразу заработал.

**Итоговые измерения (после исправлений, датчик на атмосфере, USB подключён):**

| Величина | Значение |
| :--- | :--- |
| Vcc на датчике (мультиметр) | 5.00 В |
| Vout на атмосфере (мультиметр) | **0.85 В** — совпадает с ожиданием README |
| ADC raw (усреднено, наивная формула 3.3В/4095) | ~941 |
| ADC volts (наивная формула `raw × 3.3/4095`) | ~0.758 В |
| Расхождение ADC vs мультиметр | **~11%** — больше целевых 2% |

**Вывод:** расхождение стабильно повторилось на двух независимых замерах (до и
после переворота датчика, в разных абсолютных точках — ~0.47/0.42В и
0.85/0.758В дают то же самое соотношение ~11%), значит это не шум и не плохой
контакт, а систематическая ошибка допущения "опорное АЦП = 3.3В ровно".

**Оценка реального опорного напряжения АЦП:**
```
V_ref_эффективное = 3.3 × (0.85 / 0.758) ≈ 3.70 В
```

Использовано в `firmware/src/sensor.cpp` (`adcReferenceVolts()`) вместо
номинальных 3.3В.

**Функциональная проверка:** датчик реагирует на приложенное давление (палец на
порту) — выход растёт/падает предсказуемо. Электрическая целостность всех трёх
проводов (Vout, GND, Vcc) подтверждена мультиметром (<1 Ом на землю/сигнал,
честные 5.00В на питании).

**Дальше:** после прошивки `firmware/` — перепроверить итоговое совпадение
ADC/мультиметр с новым опорным (цель — ≤2%), записать результат ниже.

## 13.09.26 — проверка ADC vs мультиметр с V_ref=3.70В (README часть 2, требование ≤2%)

**Сборка:** та же (датчик на атмосфере, USB подключён, полная прошивка
`firmware/`, порт COM6, 115200 бод), давление на экране показывает
относительное значение (обнулено при старте от атмосферного baseline),
поэтому для проверки использовано сырое поле `"adc"` из JSON-телеметрии, а
не откалиброванное `"p"`.

**Мультиметр (Vout датчика относительно GND):** 0.85 В

**ADC:** 14 подряд идущих строк телеметрии, значения `adc`:
945, 946, 947, 946, 939, 951, 946, 946, 939, 946, 945, 945, 949, 943
— среднее ≈ 945.2 (разброс 939–951, т.е. 12 LSB пик-пик, насос выключен —
пригодится позже для замера шума).

**Перевод в вольты** (`вольты = ADC × V_ref / 4095`):
```
V_ref = 3.70 В (измеренное опорное, см. выше):
  945.2 × 3.70 / 4095 ≈ 0.854 В
  разница с мультиметром: |0.854 - 0.85| / 0.85 ≈ 0.5%   → ПРОШЛО (≤2%)

для сравнения, номинальные 3.3 В (старое допущение):
  945.2 × 3.30 / 4095 ≈ 0.762 В
  разница с мультиметром: ≈ 10.4%                          → не прошло бы
```

**Вывод:** требование README части 2 (ADC в пределах 2% от мультиметра)
выполнено с измеренным V_ref = 3.70 В. Подтверждает, что замена номинала
3.3В → 3.70В в `firmware/src/sensor.cpp` (`adcReferenceVolts()`) была
правильным решением, а не разовой случайностью на старых данных.

## 13.09.26 — проверка частоты лога (README часть 2, требование 1000±5 строк за 10с)

**Метод:** слушали COM6 (115200 бод) 10 секунд подряд, считали количество
JSON-строк телеметрии.

**Результат:** 1002 строки за окно; по собственным таймстемпам прошивки
(поле `"t"`) первая строка `t=129781`, последняя `t=139791`, разница
10010 мс — то есть частота ≈100.1 Гц.

**Вывод:** 1002 строки укладывается в требуемые 1000±5 → **ПРОШЛО**.
10 мс цикл прошивки (часть 2 задания) работает как задумано.

## Па на шаг АЦП (MPX5700AP, с измеренным V_ref)

Чистый расчёт по передаточной функции датчика (README часть 2:
`Vout = Vcc·(0.0012858·P + 0.04)`) и измеренному опорному АЦП, без нового
железа — можно посчитать сразу:

```
dP/dVout = 1 / (Vcc · slope) = 1 / (5 × 0.0012858) ≈ 155.55 кПа/В

Шаг АЦП (номинал 3.3В, как в README): 3.3/4095 × 155.55 ≈ 0.1253 кПа ≈ 125 Па/шаг
Шаг АЦП (измеренный V_ref = 3.70В):   3.70/4095 × 155.55 ≈ 0.1405 кПа ≈ 141 Па/шаг
```

**Вывод:** с реальным (более высоким, чем номинал) опорным напряжением
разрешение получается **хуже**, чем в номинальном допущении README (~141
Па/шаг вместо ~125 Па/шаг) — на ~13% грубее. Это отдельный аргумент к части
1 (выбор датчика): если понадобится точность лучше текущей, придётся либо
подтянуть опорное напряжение ближе к номиналу аппаратно, либо взять датчик
с более узкой шкалой (см. TODO ниже).

## 13.09.26 — шум в LSB, pump выключен vs pump включён (README часть 2)

**Сборка:** трубка от собственного (не ACU2-B) MG400 pump box подключена
напрямую в штуцер датчика (без тройника, без присоски/стекла — по совету
преподавателя, т.к. для шумового теста удержание вакуума не нужно, только
факт работы насоса).

**Первая попытка (не получилась, зафиксировано как есть):** насос запущен
через HTTP API `mg400-base` (`POST /api/pump {"mode":"suck"}`, DO2=suck по
умолчанию пакета). За 3 секунды среднее ADC почти не изменилось (941.7 →
941.5, атмосферное давление), хотя команда прошла без ошибки
(`errid:0, digital_out:2`). Вероятная причина: неплотное соединение
трубки или неверное предположение о DO-линии (в документации `mg400-base`
прямо сказано, что DO2=suck/DO1=blow — это догадка из чужой установки,
нужно проверять по факту). Не пересчитывали дальше — см. вторую попытку.

**Вторая попытка (успешная): физическая кнопка вакуума на коробке.**
Вместо API команду дал человек — нажал кнопку вакуума на самой коробке,
пока велась запись Serial-телеметрии (10 с окно).

| Состояние | n | среднее ADC | давление (отн. атмосферы) | шум (std) | min–max |
| :--- | ---: | ---: | ---: | ---: | :--- |
| До нажатия (атмосфера, "pump выкл") | 272 | 941.6 | ≈ 0 кПа | **4.84 LSB** | 930–954 |
| После нажатия (вакуум держится) | 401 | 361.1 | **≈ -81.7 кПа** | **3.68 LSB** | 349–372 |

Переход от атмосферы до почти финального значения — **~190 мс**, резкий
чистый скачок без промежуточных ступенек (в отличие от первой неудачной
попытки).

**Вывод и нюанс:** -81.7 кПа хорошо сходится с ранее измеренными
holding-curve результатами части 4 (napp klaasil ≈ -83 кПа, voolik korgiga
≈ -83.3 кПа) — подтверждает, что на этот раз соединение было герметичным.
Шум оказался НИЖЕ во время удержания вакуума (3.68 LSB), чем в состоянии
покоя (4.84 LSB) — это не противоречие, а следствие механики коробки: как
и в случае с заводским ACU2-B (часть 3), клапан держит вакуум пассивно
после достижения цели, мотор насоса не крутится постоянно. То есть это
сравнение "атмосфера / насос неактивен" против "вакуум удержан / насос
тоже неактивен", а не буквально "мотор работает". Настоящий шум
*активно работающего* мотора виден только в те ~190 мс самого перехода —
слишком короткое окно для отдельной статистики; если для защиты
понадобится именно шум при активной работе мотора, нужен более длинный
непрерывный цикл откачки (например, зафиксировать кнопку нажатой дольше
или воспроизвести это на менее герметичной системе, чтобы мотор не
останавливался).

## 13.09.26 — Falstad-схема: датчик → ADC со своим источником шума

**Осциллограф не был доступен в этой сессии** — спектр с подписанными
пиками (мотор насоса, сервоприводы MG400, USB-питание, 50 Гц) осознанно
пропущен, README-требование по этому пункту **не выполнено**. Вместо
этого собрана только Falstad-симуляция (ниже), без реального
спектрального подтверждения.

**Что сделано:** взяли стартовую схему из README (Andur 0,2–5V → ADC
muundur 0–3,3V), вставили последовательно между источником-датчиком и
входом ADC-модели источник шума (AC, `Voltage Source (2-terminal)`):

- **Амплитуда: 4.4 мВ** — взято из реально измеренного шума покоя
  (baseline atmosphere, замер выше: std ≈ 4.84 LSB × 3.70В/4095 ≈ 4.4 мВ).
- **Частота: 50 Гц** — **предположение, НЕ подтверждено экспериментально**
  (типичная сетевая наводка, но без осциллографа нельзя сказать, что
  реальный шум датчика действительно на этой частоте — это просто
  правдоподобная заглушка).
- DC offset: 0, Phase: 0, Internal resistance: 0.

**Результат симуляции:** на выходе ADC-модели (осциллограф "output")
видна квадратная волна (обрезание сигнала 0,2–5В датчика в диапазон
0–3,3В) с лёгкой рябью 50 Гц поверх плоских участков — качественно похоже
на то, что ожидается от наложенного шума, но это **подтверждает только
что модель настроена так, как мы её настроили** (амплитуда взята из
нашего же замера), а не независимая проверка реального шума датчика.

**Живая схема (ссылка):**
https://www.falstad.com/circuit/circuitjs.html?ctz=DwYwlgTgBAZgvAIgAwKgFwM6KQOiUgRlTBEQNwCZ8KB2GgZiQA4KA2ATnZtRACNEArCigAHfgiGoAbhEGoAtpkEBTALQEiAPgBQUKMClQAHmVZIojACxQCZmzVap4yVAHdnRWLISf5AQyMpRAocS1ReMD8sBBDHAHodPWAAc2NTcysbO3p6R1hsBATdfT80hEzbc0t8LOFnJgU-RHocegUwAqhkpp98PsLE-VcyistrCoIKJwKipOGTcqRrDQooavMV6ZdZobL1m0m1msqtlB3gecR9yrX2VlrTgeLgAHk9u9qoATGH-J9UDCkbaDYBGPbmCg--TlhsApsttvaYBxcAQHRAA

**Картинка (снимок экрана схемы + оба осциллографа):**
`docs/lab1_falstad_adc.png` (README-ссылка на этот файл раньше была
битой, файла не существовало — теперь исправлено.)

---

## 13.09.26 — Выбор датчика (часть 1 задания)

**Задача:** пумба-ящик даёт -70…+110 кПа (31…211 кПа абсолютно). Нужно
сравнить с числами: чувствительность мВ/кПа, Па на шаг АЦП, % использования
шкалы, и выход против 3,3В (можно ли вообще напрямую в ADC без делителя —
делитель/ОУ по условию части 2 этой лабы использовать ещё нельзя, они
приходят только в лабе 2).

Источники данных ниже — реальные даташиты NXP (ссылки в конце раздела),
кроме одного места, отмеченного отдельно как неподтверждённое.

### Кандидаты

| Кандидат | Тип | Диапазон датчика | Чувствительность | Наше окно (-70…+110 кПа) → Vout | % шкалы датчика | Прямо в ADC (≤3.3-3.6В)? |
| :--- | :--- | :--- | ---: | :--- | ---: | :--- |
| **MPX5700AP** (уже стоит) | абсолютный | 15–700 кПа абс. | 6.43 мВ/кПа (@5В) | 0.40–1.56 В | 25.7% | ✅ да, безопасно |
| MPX4250AP | абсолютный | 20–250 кПа абс. | 20 мВ/кПа (@5В) | 0.42–4.02 В | 78.3% | ❌ выходит за 3.3В |
| MPX5100GP (плюс) | манометрический | 0–100 кПа (только +) | 45 мВ/кПа (@5В) | до ~4.7–5.15 В (за пределом шкалы датчика на +110!) | 100%+ (не влезает) | ❌ выходит за 3.3В, и физически не покрывает +110 кПа |
| MPXV6115V (вакуум) | вакуумный | -115…0 кПа | 38.26 мВ/кПа | ≈1.9–4.6 В* | 60.9% | ❌ выходит за 3.3В (по оценке) |

*\* MPXV6115V: в поиске нашлась только чувствительность (38.26 мВ/кПа) из
реального даташита, точный офсет передаточной функции (постоянная
составляющая) не подтверждён напрямую — цифра в таблице оценочная, по
типовой структуре формулы NXP для этой линейки. **Перед заказом это надо
свериться с полным PDF даташита**, но даже с запасом на ошибку сенсор явно
работает возле верхней части своей шкалы (near-full-scale около
атмосферного нуля) — вывод про "не влезает в 3.3В" устойчив к этой
неопределённости.

### Расчёт Па/шаг АЦП (наш V_ref = 3.70 В, часть 2)

```
Па/шаг = (V_ref/4095) × (1/чувствительность) × 1000

MPX5700AP:  (3.70/4095) × (1/0.00643) × 1000 ≈ 141 Па/шаг
MPX4250AP:  (3.70/4095) × (1/0.020)   × 1000 ≈  45 Па/шаг
MPX5100GP:  (3.70/4095) × (1/0.045)   × 1000 ≈  20 Па/шаг
MPXV6115V:  (3.70/4095) × (1/0.03826) × 1000 ≈  24 Па/шаг
```

Все три альтернативы дают заметно более тонкое разрешение (20-45 Па/шаг
против 141 Па/шаг у MPX5700AP) — на бумаге они выигрывают в 3-7 раз.

### Главный вывод — почему решение НЕ в пользу лучшего разрешения

Все три альтернативы (MPX4250AP, MPX5100GP, MPXV6115V) при работе именно
в нашем диапазоне (-70…+110 кПа) выдают напряжение **выше 3.3-3.6В** —
то есть выше того, что ESP32-ADC-пин безопасно выдерживает напрямую (см.
README, раздел "Ohutus": *"ADC viik ei kannata 5V; andur, mille väljund
käib 4,7V-ni, ei lähe täisskaalal otse viiku"*). Чтобы их использовать,
понадобился бы делитель или ОУ — а часть 2 этой лабы прямо запрещает их
ставить сейчас ("ei jagurit, ei op-ampi, ei filtrit... Labor 2 paneb
vahele kõigepealt jaguri").

**MPX5700AP — единственный кандидат, чьё реальное рабочее окно (0.40–1.56В)
безопасно помещается в ADC без всякой дополнительной схемы.** Его
разрешение (141 Па/шаг, 25.7% шкалы) — худшее из четырёх, но это
осознанная цена за прямое подключение без делителя, а не недосмотр.

**Решение: остаёмся на MPX5700AP для этой лабы**, с числами выше как
обоснованием. Если/когда в лабе 2 появится делитель/ОУ — MPX4250AP
(45 Па/шаг, 78% шкалы, один датчик вместо двух) стоит пересмотреть как
следующий шаг: он даёт заметно лучшее разрешение и остаётся одним
абсолютным датчиком (не нужно городить пару манометрических + логику
переключения между ними на 0 кПа).

## 13.09.26 — Спектр (осциллограф GW Instek GDS-1072A-U), попытка

**Осциллограф получили в тот же день** (после того, как выше было записано
"осциллографа не было" — актуально только для более ранней части этой же
сессии). Попытались закрыть требование части 2 про спектр с подписанными
пиками. Итог: **частично, с честными оговорками ниже** — чисто-выделенных
именованных пиков (мотор/сеть/USB) получить не удалось, но качественная
корреляция "насос вкл → шум растёт" подтверждена.

**Сборка:** щуп CH1 (x1) на Vout датчика, земля щупа на GND-рейку, Coupling
= AC (чтобы убрать постоянные 0,85В и видеть только шум), FFT через Math
(Source CH1, Window Flattop).

**Проблема с масштабом:** в процессе настройки масштаб несколько раз
"съезжал" — то из-за случайных прикосновений к не той ручке, то
осознанно (коллега выставил Volts/Div = 5В/дел, ориентируясь на питание
платы Vcc=5В — но для AC-coupled шума в несколько милливольт это слишком
грубо, сигнал почти не виден на таком масштабе). Time/Div тоже не
зафиксировался стабильно на нужных 2-5 мс (то и дело съезжал в
микросекунды). В итоге:
- Одна FFT-картинка снята при Time/Div=25µs → диапазон частот на экране
  ~0-2,48 МГц (гораздо шире, чем нужно для 50Гц/мотора) — на ней виден
  общий спадающий с частотой "горб" шума слева (низкие частоты), без
  чёткого одиночного пика, который можно было бы уверенно назвать (сеть
  50Гц или мотор конкретно) — на таком широком диапазоне и с 5В/дел
  разрешения не хватает.

**Что удалось подтвердить (качественно, не по частоте):** при включении
насоса (кнопка на коробке) сырой сигнал CH1 (жёлтая линия, до FFT)
визуально "оживает" — амплитуда колебаний заметно растёт, линия чаще
скачет вверх-вниз (не постоянно, а именно при включённом насосе). При
выключении насоса — обратно успокаивается. Это подтверждённая
корреляция "источник (насос) → рост шума", просто **без точной частоты**
этого шума — то есть половина требования (подтверждение включением/
выключением источника) выполнена, а другая половина (назвать частоту
пика) — нет.

**Честный итог по этому пункту:** спектр с уверенно **подписанными,
поимённо названными** пиками (мотор насоса на конкретных Гц, сервоприводы
MG400, USB-питание, 50Гц) **не получен** — помешал не сам факт отсутствия
осциллографа (он появился), а то, что не удалось стабильно удержать
правильный масштаб (мВ и миллисекунды) в течение всей сессии. Что реально
есть: качественное видео/визуальное подтверждение, что насос — реальный
источник дополнительного шума (амплитуда растёт и падает вместе с
включением/выключением), без точной частотной привязки. Если будет ещё
время до защиты — повторить с осциллографом, подключенным по USB к
компьютеру (кабель USB-A↔USB-B, порт "USB Device" на задней панели
GDS-1072A-U) для прямого управления масштабом и выгрузки данных вместо
ручной подгонки кнопками.

**Источники (реальные даташиты, проверено 13.09.26):**
- MPX5700AP: [nxp.com/docs/en/data-sheet/MPX5700.pdf](https://www.nxp.com/docs/en/data-sheet/MPX5700.pdf) (уже в README)
- MPX4250AP: [nxp.com/docs/en/data-sheet/MPX4250A.pdf](https://www.nxp.com/docs/en/data-sheet/MPX4250A.pdf)
- MPX5100GP: сенситивность и диапазон подтверждены через листинг element14
  (45 мВ/кПа, 0-100 кПа, 4.75-5.25В) — датасшит [nxp.com/docs/en/data-sheet/MPX5100.pdf](https://www.nxp.com/docs/en/data-sheet/MPX5100.pdf)
- MPXV6115V: [nxp.com/docs/en/data-sheet/MPXV6115V.pdf](https://www.nxp.com/docs/en/data-sheet/MPXV6115V.pdf) (сенситивность 38.26 мВ/кПа подтверждена, офсет — нет, см. оговорку выше)
